import { StyleSheet } from 'react-native';

const requestRecStyles = StyleSheet.create({
  container: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: '#E6DE9C',
  }
})

export default requestRecStyles;
